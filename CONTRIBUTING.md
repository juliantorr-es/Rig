# Contributing

Keep changes small and path-safe.

- Prefer edits under `src/`.
- Preserve `scripts/rig.py` compatibility until all internal references are repaired.
- Run `scripts/ci/check.sh` before asking for review.
- Document unresolved workspace assumptions in `docs/migration/rig-repo-migration.md`.
