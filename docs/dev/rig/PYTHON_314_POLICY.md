# Python 3.14 Policy

Rig requires Python 3.14 or newer for the supported local toolchain.

- Use Python 3.14 for CLI, UI, tests, and validation.
- Avoid interpreter drift from older Homebrew or user-site shims.
- Prefer explicit `python3.14` or a venv created from Python 3.14.
