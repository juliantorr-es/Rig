# TUI Decomposition

The TUI is being split into smaller components:

- command specs
- command planning
- rendering helpers
- snapshot loading
- theme/CSS

The TUI remains a projection and control surface. It does not become the source of truth.
