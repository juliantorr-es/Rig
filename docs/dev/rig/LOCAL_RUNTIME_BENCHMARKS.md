# Local Runtime Benchmarks

Document local benchmark commands and acceptance thresholds for Rig runtime surfaces.

- Keep benchmark runs local and deterministic.
- Record tool versions used for each benchmark run.
- Treat benchmark output as evidence, not authority.
