# Cloud API Harness

Cloud APIs are proposal providers, not execution authorities.

Rig builds a governed context packet, sends that packet to a provider, decodes the response into `rig.agent_proposal.v1`, and only then allows the normal workspace governance pipeline to continue.

No provider may mutate the repository directly.
