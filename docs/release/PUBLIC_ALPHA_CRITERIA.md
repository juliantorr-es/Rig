# Public Alpha Criteria

Rig is pre-1.0. The public API and CLI may still change.

Public alpha requires:

- Python 3.14+
- no auto-apply
- no provider direct mutation
- safe debug bundle export
- documented install path
- predictable `rig` command surface
