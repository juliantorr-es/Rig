"""
UI Backend implementations for rig_tools.

These are the implementation-layer backends that handle actual UI
operations (TUI, Web, etc.). They implement the internals while the 
domain layer provides the public interface via protocols.
"""

# Currently, UI backends are placeholders
# The actual implementations will be added when specific UI requirements are clear

__all__ = []
